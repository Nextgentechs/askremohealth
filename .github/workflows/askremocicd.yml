name: 🚀 Deploy to Staging & Merge to Main

on:
  push:
    branches:
      - 'Staging.env'  # Trigger on push to Staging.env
  pull_request:
    branches:
      - 'main'         # Trigger on pull request to main branch

jobs:
  deploy-staging:
    name: Deploy to Staging Subdomain and Merge to Main
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: 🚀 Deploy to Staging via SSH (systemd)
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            echo "🔄 Starting STAGING deployment..."
            cd /root/askremohealth-staging

            # Pull the latest code from Staging.env
            git checkout Staging.env
            git pull origin Staging.env

            # Install dependencies and build the app
            echo "📦 Installing dependencies..."
            npm install
            echo "🛠️ Building Next.js app..."
            rm -rf apps/web/.next
            npm run build --workspace=apps/web

            # Restart systemd service
            echo "🔄 Restarting systemd service for Staging..."
            systemctl restart askremohealth-staging.service
            systemctl status askremohealth-staging.service

            # Health check on port 3001
            echo "⏳ Waiting for app to start..."
            sleep 10
            if curl -f http://localhost:3001; then
              echo "✅ Staging deployment successful!"
            else
              echo "⚠️ Staging site not responding on port 3001."
              exit 1
            fi

            echo "🎉 Staging deployment completed."

      - name: Merge Staging.env into Main (on successful Staging Deployment)
        if: github.ref == 'refs/heads/Staging.env'
        run: |
          echo "🔀 Merging changes from Staging.env to Main..."
          git checkout main
          git pull origin main
          git merge Staging.env --no-edit
          git push origin main
