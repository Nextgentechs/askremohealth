name: 🚀 Deploy AskRemoHealth-main dormain
on:
  workflow_run:
    workflows: ['🚀 Promote Staging → Main (Manual Approval)']
    types:
      - completed
  push:
    branches:
      - main

jobs:
  deploy:
    name: 📦 Deploy to Askremohealth prod
    runs-on: ubuntu-latest

    steps:
      - name: 📝 Log trigger reason
        run: echo "Triggered by workflow_run or push"
      - name: 📥 Checkout Code
        uses: actions/checkout@v3

      - name: 🚀 Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # =============================================================================
            # 🚀 Full Deployment Script for AskRemoHealth
            # Purpose: Deploy latest code, rebuild, restart PM2, reload Nginx
            # =============================================================================

            set -e
            echo "🔄 Starting deployment..."

            # 1. Navigate to App Directory
            cd /root/askremohealth
            echo "📁 Changed to /root/askremohealth"

            # 2. Force sync with origin/main (handles divergent branches)
            echo "📥 Fetching and resetting to origin/main..."
            git fetch origin main
            git checkout main
            git reset --hard origin/main
            echo "✅ Synced with origin/main"

            # 3. Install Dependencies
            echo "📦 Installing npm dependencies..."
            npm install

            # 4. Build the Next.js App
            echo "🧹 Cleaning build caches..."
            rm -rf apps/web/.next
            rm -rf .next
            rm -rf .turbo
            rm -rf node_modules/.cache
            echo "🛠️ Building Next.js app..."
            npm run build --workspace=@askremohealth/web

            # 5. Setup Standalone Output for PM2
            echo "📋 Copying static assets to standalone directory..."
            cp -r apps/web/.next/static apps/web/.next/standalone/apps/web/.next/static
            cp -r apps/web/public apps/web/.next/standalone/apps/web/public

            # 6. Restart PM2 with Standalone Server
            echo "🔄 Starting PM2 with standalone server..."
            cd apps/web/.next/standalone/apps/web
            pm2 delete askremohealth-web 2>/dev/null || true
            pm2 start server.js --name askremohealth-web --env production
            pm2 save
            cd /root/askremohealth

            # 7. Reload Nginx
            echo "🔁 Reloading Nginx..."
            nginx -t && systemctl reload nginx
            if [ $? -eq 0 ]; then
              echo "✅ Nginx reloaded successfully"
            else
              echo "❌ Nginx config error! Check with: nginx -t"
              exit 1
            fi

            # 8. Final Health Check
            echo "⏳ Waiting for app to start..."
            sleep 30

            echo "🔍 Checking PM2 status..."
            pm2 status askremohealth-web

            echo "🔍 Checking if app is responding on port 3000..."
            # Accept any response (including 404) - app is running if we get a response
            if curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 | grep -E "^[2345]"; then
              echo "✅ Deployment successful! App is responding."
            else
              echo "⚠️ Port 3000 not responding. Checking PM2 logs..."
              pm2 logs askremohealth-web --lines 20 --nostream
              exit 1
            fi

            echo "🎉 Deployment completed."
