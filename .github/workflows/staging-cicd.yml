name: 🚀 Deploy to Staging & Merge to Main

on:
  push:
    branches: ['staging-config']
  pull_request:
    branches: ['main']

permissions:
  contents: write

jobs:
  deploy-staging:
    name: Deploy to Staging Subdomain and Merge to Main
    runs-on: ubuntu-latest

    env:
      BRANCH: staging-config
      REPO_DIR: /root/askremohealth-staging          # ← change if your path differs
      SERVICE: askremohealth-staging.service         # ← change if your service differs
      HEALTH_URL: http://localhost:3001

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🚀 Deploy to Staging via SSH (systemd)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script_stop: true
          script: |
            set -Eeuo pipefail
            trap 'echo "❌ Failed at line $LINENO. Last cmd: $BASH_COMMAND"' ERR
            set -x

            echo "🔄 Starting STAGING deployment..."
            BRANCH="${BRANCH:-staging-config}"
            REPO_DIR="${REPO_DIR:-/root/askremohealth-staging}"
            GIT_URL="git@github.com:Nextgentechs/askremohealth.git"

            # Ensure repo exists
            if [ ! -d "$REPO_DIR/.git" ]; then
              mkdir -p "$REPO_DIR"
              rm -rf "$REPO_DIR"
              git clone --origin origin --branch "$BRANCH" "$GIT_URL" "$REPO_DIR"
            fi

            cd "$REPO_DIR"
            test "$(pwd)" = "$REPO_DIR"

            git remote set-url origin "$GIT_URL"

            # Heal/refresh remote-tracking refs (prevents 'cannot lock ref ... expected ...')
            git remote prune origin || true
            git update-ref -d "refs/remotes/origin/$BRANCH" || true
            git fetch --prune origin "+refs/heads/*:refs/remotes/origin/*"

            # Verify the remote branch exists
            git rev-parse --verify "refs/remotes/origin/$BRANCH"

            # Wrapper-safe local branch existence check (never exits non-zero)
            if [ -n "$(git show-ref --heads -s "$BRANCH" 2>/dev/null || true)" ]; then
              git checkout "$BRANCH"
            else
              git checkout -B "$BRANCH" "refs/remotes/origin/$BRANCH"
            fi

            # Ensure working tree matches remote exactly
            git reset --hard "refs/remotes/origin/$BRANCH"

            echo "📦 Installing dependencies..."
            if [ -f package-lock.json ]; then
              npm ci || npm install
            else
              npm install
            fi

            echo "🛠️ Building Next.js app..."
            rm -rf apps/web/.next || true
            npm run build --workspace=apps/web

            echo "🔄 Restarting systemd service for Staging..."
            systemctl restart "${SERVICE}"
            systemctl status "${SERVICE}" --no-pager || true

            echo "⏳ Health check (retries)..."
            for i in $(seq 1 12); do
              if curl -fsS "${HEALTH_URL}" >/dev/null; then
                echo "✅ Staging deployment healthy at ${HEALTH_URL}"
                break
              fi
              echo "⏳ Attempt $i/12: service not ready yet; retrying in 5s..."
              sleep 5
            done
            # Final assert
            curl -fsS "${HEALTH_URL}" >/dev/null

            echo "🎉 Staging deployment completed."

      - name: 🔀 Merge staging-config into main (only after successful deploy)
        if: github.ref == 'refs/heads/staging-config'
        run: |
          set -Eeuo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git fetch origin --prune
          git checkout main
          git reset --hard origin/main

          # Merge the remote branch we just deployed
          git merge --no-edit origin/staging-config || true

          git push origin main
